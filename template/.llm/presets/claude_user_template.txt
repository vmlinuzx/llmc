CONTRACTS:
- minisite.states = [disabled, preview, live]
- routing.policy = live => 302 /clubs/{slug} -> minisite_url
- security.pii = false

TASK:
Apply the attached DIFF to implement X. Only touch shown hunks unless a compile error would result.

CONTEXT:
- CHANGED_HUNKS: <...minimal slices...>
- HALO: <interfaces|callers|tests>
ACCEPTANCE:
- tests: <names>
- build: <targets>
LIMITS:
- output <= 400 tokens
- format = PATCH + RATIONALE
