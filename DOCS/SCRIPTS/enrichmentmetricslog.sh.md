# enrichmentmetricslog.sh — Watch Enrichment Metrics

Path
- scripts/enrichmentmetricslog.sh

Purpose
- Run `watch -n 10 scripts/summarize_enrichment_metrics.py` from the repo root.

Usage
- `scripts/enrichmentmetricslog.sh`

Notes
- Requires `watch` (procps) and Python available for the summarizer.

