# llmc_edit.sh — Apply Changesets with Guardrails

Path
- scripts/llmc_edit.sh

Purpose
- Internal helper used by `codex_wrap.sh` to apply an AI‑proposed changeset (`--changeset <path>`), honoring repo guardrails and emitting structured logs.

Usage
- Typically invoked by other wrappers; run `scripts/llmc_edit.sh --help` for current flags.

