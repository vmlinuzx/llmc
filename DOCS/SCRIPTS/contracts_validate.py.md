# contracts_validate.py — Sidecar Drift/Checksum Validation

Path
- scripts/contracts_validate.py

Purpose
- Validate the generated sidecar against expectations and expose a slice renderer compatible with multiple vendors.

Usage
- `python3 scripts/contracts_validate.py` (used in precommit), see `scripts/contracts_render.py` for slicing.

