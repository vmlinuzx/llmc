# integration_gate.sh — Integration Gate Harness

Path
- scripts/integration_gate.sh

Purpose
- Orchestrate integration checks (builds/tests/smoke) before merging or releasing. Emits a single pass/fail gate.

Usage
- `scripts/integration_gate.sh` (see script for specific checks enabled in this repo)

