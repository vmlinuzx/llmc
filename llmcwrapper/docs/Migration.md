# Migration: from shell wrappers to llmcwrapper

- Replace `claude_wrap.sh --yolo` → `llmc-yolo`.
- Replace `claude_wrap.sh` default path → `llmc-rag`.
- Keep old shells as one-liners for 2 weeks with a deprecation banner.
