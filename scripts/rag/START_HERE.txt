═══════════════════════════════════════════════════════════════
  DEEPSEEK RAG SYSTEM - START HERE
═══════════════════════════════════════════════════════════════

📍 YOU ARE HERE:
   ~/src/glideclubs/scripts/rag/

📖 READ THIS FIRST:
   cat README.md
   
   (Full documentation with examples, troubleshooting, everything)

🚀 QUICK SETUP (3 commands):

   1. Make setup script executable:
      chmod +x setup_rag.sh
   
   2. Run it:
      ./setup_rag.sh
      
      (Takes 5-10 minutes to index your workspace)
   
   3. Test it:
      python query_context.py "authentication"

✅ THAT'S IT! You now have:
   - RAG system indexing all ~/src/ projects
   - Vector search across your entire codebase  
   - Auto-context for DeepSeek
   - (Legacy) Web UI at http://localhost:8765  [deprecated / unsupported]

═══════════════════════════════════════════════════════════════

📚 FILES IN THIS DIRECTORY:

   README.md ................ Complete documentation (READ THIS)
   QUICK_START.txt .......... Quick reference card (this file)
   setup_rag.sh ............. One-command setup
   requirements.txt ......... Python dependencies
   index_workspace.py ....... Index your ~/src/ workspace
   query_context.py ......... Query the RAG system
   watch_workspace.py ....... Auto-reindex file changes
   rag_server.py ............ Deprecated legacy Web UI server (not supported)

═══════════════════════════════════════════════════════════════

❓ WHAT DOES IT DO?

   Before: DeepSeek has no idea about your code
   After:  DeepSeek knows your entire workspace!
   
   Example:
   
   You: "How do we handle authentication?"
   
   RAG finds:
   - lib/auth.ts
   - app/api/auth/route.ts
   - middleware.ts
   - YOUR actual patterns
   
   DeepSeek answers using YOUR code as context.
   
   Result: Answers that fit your codebase perfectly.

═══════════════════════════════════════════════════════════════

🎯 NEXT STEPS:

   1. Read README.md for full details
   2. Run ./setup_rag.sh to index workspace
   3. Test with: python query_context.py "test query"
   4. (Legacy) Start web UI: python rag_server.py
      NOTE: This web UI is deprecated / unsupported and may not work.
   5. Use normally with: ../llm_gateway.sh --local "task"

═══════════════════════════════════════════════════════════════

💡 TIP: After setup, RAG works automatically with llm_gateway!

   cd ~/src/glideclubs
   echo "your task" | ./scripts/llm_gateway.sh --local
   
   DeepSeek will automatically use RAG context.

═══════════════════════════════════════════════════════════════
