# run_mcpo_bridge.sh — Launch MCP Bridge

Path
- scripts/run_mcpo_bridge.sh

Purpose
- Start the configured Model Context Protocol bridge for desktop/FS agents as part of local orchestration.

Usage
- `scripts/run_mcpo_bridge.sh` (details depend on the configured MCP server)

