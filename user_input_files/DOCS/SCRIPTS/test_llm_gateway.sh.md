# test_llm_gateway.sh — Gateway Smoke Tests

Path
- scripts/test_llm_gateway.sh

Purpose
- Exercise the LLM gateway via: stdin pipe, CLI arg, and forced `--api` route.

Usage
- `scripts/test_llm_gateway.sh`

Notes
- Requires the gateway and at least one backend to be reachable; output is printed inline.

